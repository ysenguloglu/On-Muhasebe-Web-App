# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Excel İşlemleri
pandas>=2.0.0
openpyxl>=3.1.0

# HTTP İstekleri (PDF oluşturma için html2pdf.app API)
requests>=2.31.0

# Gmail API
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
google-api-python-client>=2.100.0

# Environment Variables
python-dotenv>=1.0.0

# Not: PDF oluşturma için html2pdf.app API kullanılmaktadır (harici servis)
# Not: E-posta gönderme için smtplib (Python standart kütüphanesi) kullanılmaktadır
# Not: Veritabanı için sqlite3 (Python standart kütüphanesi) kullanılmaktadır
