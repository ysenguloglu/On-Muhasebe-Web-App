# PDF Oluşturma API Ayarları
# html2pdf.app API key'inizi buraya ekleyin
PDF_API_KEY=api keyiniz

# SMTP E-posta Ayarları
# Gmail için varsayılan değerler kullanılıyor, farklı bir SMTP sunucusu kullanıyorsanız değiştirin
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# SMTP Kullanıcı Bilgileri
# Gmail kullanıyorsanız, 2 Adımlı Doğrulama aktif olmalı ve Uygulama Şifresi oluşturulmalı
# Uygulama şifresi: https://myaccount.google.com/apppasswords
SMTP_USER=mail
SMTP_PASSWORD=app pass

# E-posta Adresleri
# Gönderen e-posta adresi (genellikle SMTP_USER ile aynı)
EMAIL_FROM=gönderen mail
# Alıcı e-posta adresi
EMAIL_TO=alıcı mail

# Gmail API Ayarları (SMTP yerine - Render.com port kısıtlamaları nedeniyle)
# Gmail API kullanmak için OAuth 2.0 credentials gerekir
# Kurulum: https://developers.google.com/gmail/api/quickstart/python

# Yöntem 1: OAuth Client Credentials JSON (tek seferlik setup için)
# Google Cloud Console'dan indirdiğiniz credentials.json dosyasının içeriğini buraya ekleyin
# GMAIL_CREDENTIALS_JSON={"installed":{"client_id":"...","client_secret":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","redirect_uris":["..."]}}

# Yöntem 2: Refresh Token (production için - önerilen)
# İlk kurulumdan sonra oluşturulan token'ı buraya ekleyin
# GMAIL_TOKEN_JSON={"token":"...","refresh_token":"...","token_uri":"...","client_id":"...","client_secret":"...","scopes":["https://www.googleapis.com/auth/gmail.send"]}
